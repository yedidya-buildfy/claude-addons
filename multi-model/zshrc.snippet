# multi-model: when on, `claude` shows every provider's models (toggle with
# `ccx on` / `ccx off`), and falls back to plain Claude Code if the local proxy
# is unusable. `ccx` itself is always available.
{{claudeViaCcx}}
