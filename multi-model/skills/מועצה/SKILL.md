---
name: מועצה
description: Hebrew name for /council — several models plan a question together on one shared board.
argument-hint: "<שאלה לתכנון>"
disable-model-invocation: true
---

This is `/council` under its Hebrew name. Read the `council` skill in the same
skills folder as this file (`../council/SKILL.md`) and follow it exactly, with
the question `$ARGUMENTS`.
