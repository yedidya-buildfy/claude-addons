# Agent bootstrap

Read `CLAUDE.md` before changing or installing anything in this repository.

For a fresh Mac, use the standalone multi-model installer documented there. Do not copy provider credentials, local keys, or runtime state from another machine. Verify the global `ccx` command and service status before reporting bootstrap complete.
